<?php

//1 Class
/**
 * @Class is blueprint of an object that acts as a guide to build objects that conform to its class
 */



//2 Object

/**
 *  an Object is an instance created from a blueprint that 
 *  could be either a custom class or using Object superClass
 */


//3 inheritance
/**
 *  it's one of the pillar concepts of oop , it came to help us go from something general to
 *  something specific it's not meant to be a tool for DRY principle, Like if we have a general class 
 *  Shape, with inhiretance we could specify many subtypes like Square, Rectangle,..., so it help us in polymorphisme and it give the meaning
 *  that a class is of type its parent, a square is a Shape
 */

//4 Encapsulation
/**
 * 
 * also one of core concepts of oop encapsulation let us organize the behaviors and attributes 
 * of an object in a single encapsulating class , in this way we could communucate with that object through the methods it provides
 * it come with access modifiers that let us controle the security of our class attrs and methods
 */


//5 Abstraction

/**
 * abstraction is a mean for us to define the behaviors of a class without caring about the implementation,
 * as it's gonna be different in each class that implement that method, it's like defining contracts
 * and letting the behavior later until we decide which class implements them and how to immplement them
 */


//6 Polymorphism

/**
 * is anoother strong concept in oop, that give us the posibiliy to change implementation
 * of a method or 
 */